# CLAUDE.md — youtube-topic-scout

## プロジェクト概要

YouTube新ジャンル発掘のためのマルチエージェントシステム。「視聴者の関心度が高く」「短期間で登録者が積み上がる可能性があり」「自宅で調査・とりまとめが完結し、自動生成できる（現地取材・出演不要）」というERG自身の制作条件に合うYouTubeジャンルを、複数の候補ジャンルを並列調査したうえで比較・ランキングし、企画比較スライド（pptx）として出力する。

スクール教材『Claude Codeでマルチエージェントシステムを作ろう』第11章の最終課題として作成したが、課題提出用の使い捨てではなく、ERG自身のYouTubeチャンネル運営における今後の定期的な企画調査に実運用する前提で設計している。

---

## チームの目的

指定（または未指定なら自ら発案した）候補ジャンルについて、①視聴者の関心度 ②短期間での登録者増加ポテンシャル ③自宅完結・自動生成適性（現地取材・本人出演不要）の3軸で並列調査し、比較・ランキングした企画比較スライドを全自動で生成する。

## 役割一覧

| 役割 | 定義ファイル名 | 主な担当業務 |
|------|-------------|------------|
| PM（メインエージェント） | なし（本ファイルで定義） | 候補ジャンルの発案（未指定時）・タスク分解・割り当て・進捗管理・最終報告 |
| ジャンルリサーチャー | genre-researcher.md | 候補ジャンルごとの調査（並列） |
| コンテンツストラテジスト | content-strategist.md | 調査データを3軸で比較・ランキング・推奨理由の作成 |
| スライド生成エージェント | slide-builder.md | md-to-slides Skill経由でpptx生成（multi-agent-practiceから流用） |

## 並列／逐次のルール

- **フェーズ0（PM単独）：** 候補ジャンルがユーザーから指定されていない場合、PMが「今伸びていそうな、自宅完結・自動生成に向くジャンル」を5〜8件ブレインストームする
- **フェーズ1（並列）：** 候補ジャンルの数だけgenre-researcherを同時起動（1ジャンル=1エージェント。ファイル競合なし・依存関係なしのため並列可）
- **フェーズ2（逐次）：** content-strategist が全genre-researcherの結果を統合してから実行（前工程の結果に依存するため逐次）
- **フェーズ3（逐次）：** slide-builder がcontent-strategistのランキング結果をもとにスライド生成（前工程に依存するため逐次）

## 出力先の統一規約

| 種別 | 出力先 |
|------|--------|
| 各ジャンルの調査データ | `tmp/genre-researcher/{ジャンル名（英小文字・ハイフン区切り）}.md` |
| 統合スコアカード（Markdown） | `output/{実行日YYYYMMDD}/scorecard.md` |
| 企画比較スライド | `output/{実行日YYYYMMDD}/slides/genre-scout.pptx` |
| スライド画像 | `output/{実行日YYYYMMDD}/images/slide-NN.png` |

## PMの行動指針

サブエージェントを起動する際は、必ず `.claude/agents/` 配下のカスタムサブエージェント定義ファイルを参照すること。

「新ジャンルを調査してほしい」という依頼を受けたら、以下の標準フローで各サブエージェントに作業を割り当てる。

1. **フェーズ0（候補ジャンルの確定）：**
   - ユーザーから具体的な候補ジャンルが指定されていれば、それをそのまま使う
   - 指定がなければ、PM自身が「顔出し不要・現地取材不要で、ナレーション＋映像素材だけで完結できるジャンル」を軸に、今伸びていそうな候補を5〜8件ブレインストームする（例：都市伝説・雑学解説、心理学・自己啓発の要約、歴史解説、ビジネス書要約、ランキング系、占い・スピリチュアル（九星気学を含む）、ゲーム考察・攻略まとめ、経済ニュース解説 等）。旅番組・体験レポート・本人出演の実写系ジャンルは、この時点で候補から除外する

2. **フェーズ1（ファンアウト）：** 確定した候補ジャンルの数だけgenre-researcherを同時起動
   - genre-researcher-1：候補ジャンル1の調査 → `tmp/genre-researcher/{ジャンル名1}.md`
   - genre-researcher-2：候補ジャンル2の調査 → `tmp/genre-researcher/{ジャンル名2}.md`
   - （候補数ぶん繰り返す。目安2〜8並列）

3. **フェーズ2（パイプライン）：** content-strategist を実行
   - 全genre-researcherの調査結果を読み込み、genre-scorecard Skillのフォーマットで3軸評価・ランキング
   - → `output/{実行日}/scorecard.md`

4. **フェーズ3（パイプライン）：** slide-builder を実行
   - scorecard.mdを入力に、md-to-slides Skillで企画比較スライドを生成
   - → `output/{実行日}/slides/genre-scout.pptx`

全工程完了後、成果物のパスと上位候補・推奨理由をユーザーに報告してください。

---

## 技術スタック

| 項目 | 内容 |
|---|---|
| 言語・環境 | Node.js（PptxGenJS・Playwright）／Python（スライド画像生成、multi-agent-practiceと共通） |
| バージョン管理 | GitHub |

---

## Git 運用ルール

### 基本方針
- **コードを変更するたびに GitHub へプッシュすること。**
- コミットとプッシュはセットで行う。変更をローカルに溜めない。

### コミット手順
1. 変更ファイルを確認: `git status`
2. 関連ファイルを個別にステージ: `git add <file>`（`git add .` は機密ファイル混入防止のため避ける）
3. コミット: `git commit -m "簡潔なメッセージ"`
4. プッシュ: `git push origin main`

### 禁止事項
- `--force` プッシュは原則禁止（ユーザーが明示的に指示した場合のみ）
- APIキー・Webhook URLをコードに直書きしない

---

## リモートリポジトリ

- **GitHub**: https://github.com/ergtronix/youtube-topic-scout
- デフォルトブランチ: `main`
